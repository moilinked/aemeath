---
description: Go 代码与中文注释规范
globs: "**/*.go"
alwaysApply: false
---

# Go 规范

- 使用小接口和依赖注入保持模块边界，阻塞操作必须接收 `context.Context`。
- 显式处理错误并使用 `%w` 包装；不得使用 panic 处理常规错误。
- 并发任务必须有明确的取消、退出和错误传播路径。
- 新增逻辑应提供表驱动测试，并运行 `gofmt`、`go vet ./...` 和 `go test ./...`。
- 不硬编码 API Key、模型连接参数或部署配置。

# 中文注释

- 导出的类型、接口、函数和常量必须添加简洁的中文文档注释。
- 注释优先解释设计原因、边界、兼容性和异常行为，不逐行复述代码。
- Provider 特有字段必须说明适用范围，例如 DeepSeek 的思考内容回传要求。
- API Key、Token 等敏感值不得出现在注释、日志、测试快照或错误消息中。
